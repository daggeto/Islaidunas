<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.islaidunas" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:name=".IslaidunasApp">
        <activity
            android:name=".ui.TransactionsListActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.AddTransactionActivity" android:label="@string/add_transactio_label">
            <meta-data android:name="android.support.PARENT_ACTIVITY"
                android:value=".TransactionsListActivity" />
        </activity>
    </application>

</manifest>
